export interface OfficeDto {
    id: string;
    name: string;
    address: string;
}

export interface OfficeHistoryDto {
    id: string;
    name: string;
    createdAt: string;
    updatedAt: string;
}
